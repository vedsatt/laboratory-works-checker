document.addEventListener('DOMContentLoaded', function() {
    const urlParams = new URLSearchParams(window.location.search);
    const labNumber = urlParams.get('lab') || '1';

    if (!labNumber || labNumber < 1 || labNumber > 11) {
        window.location.href = 'index.html';
        return;
    }

    document.getElementById('lab-number').textContent = labNumber;
    document.getElementById('lab-title').textContent = `Лабораторная работа №${labNumber}`;

    loadLabDescription(labNumber);

    document.getElementById('submit-btn').addEventListener('click', submitSolution);

    loadSolutionHistory(labNumber);
});

const tasksByLab = {
    '1': {
        parts: 3,
        variations: {
            part1: 7,
            part2: 9,
            part3: 10
        },
        templates: {
            part1: (variantNum) => {
                const formulas = [
                    "r[i] = 2.5 sin(ax + i²h); (i = 1..n, x, h, a заданы)",
                    "r[i] = 1.25 sin(3ax - ih); (i = 1..n, x, h, a заданы)",
                    "r[i] = 6 cos(ax + ih); (i = 1..n, x, h, a заданы)",
                    "r[i] = 0.3 cos(2ax - i²h); (i = 1..n, x, h, a заданы)",
                    "r[i] = cos(ih) - cos(ax + ih); (i = 1..n, x, h, a заданы)",
                    "r[i] = 5.5 sin(ih) + cos(ax + ih); (i = 1..n, x, h, a заданы)",
                    "r[i] = 0.8 cos(8ax - ih); (i = 1..n, x, h, a заданы)"
                ];
                return `Вычислить массив R[1:n]: <br><code>${formulas[(variantNum % 7)]}</code>`;
            },
            part2: (variantNum) => {
                const conditions = [
                    "все элементы, удовлетворяющие условию: ∣r[i]∣<0.7",
                    "все отрицательные элементы, расположенные до первого максимального элемента",
                    "все положительные элементы, расположенные после первого отрицательного элемента",
                    "все отрицательные элементы, расположенные между первым минимальным и последним положительным элементами",
                    "все положительные элементы, удовлетворяющие условию: r[i]<i/3",
                    "все положительные элементы, расположенные до последнего отрицательного элемента",
                    "все элементы, расположенные после первого отрицательного элемента и удовлетворяющие условию: ∣r[i]∣≥0.3",
                    "все элементы, расположенные между последним положительным и первым максимальным по модулю элементами",
                    "все положительные элементы, расположенные после первого минимального элемента",
                ];
                return `Из вычисленного массива R удалить:<br>${conditions[(variantNum % 9)]}`;
            },
            part3: (variantNum) => {
                const conditions = [
                    "между первым минимальным и первым отрицательным элементами",
                    "после первого максимального элемента",
                    "до последнего минимального элемента включительно",
                    "между первым максимальным и минимальным по модулю элементами",
                    "до первого отрицательного элемента",
                    "после последнего положительного элемента",
                    "между последним отрицательным и первым минимальным по модулю элементами",
                    "после последнего максимального положительного элемента",
                    "до первого минимального по модулю отрицательного элемента",
                    "между последним минимальным отрицательным и первым положительным элементами"
                ];
                return conditions[(variantNum % 10)];
            }
        }
    },
    // Добавьте аналогичные структуры для других лабораторных
};

function loadLabDescription(labNumber) {
    const descriptions = {
        '1': `<h2>Цель работы:</h2>
              <p>Изучение базовых алгоритмов обработки массивов.</p>
              <h2>Часть 1:</h2>
              <p>Вычислить массив R[1:n] в соответствии с формулами:</p>
              <h2>Часть 2:</h2>
              <p>Из вычисленного массива R удалить:</p>
              <h2>Часть 3:</h2>
              <p>В полученном массиве R[1:k], где k – число элементов, оставшихся после удаления, подсчитать среднее арифметическое элементов, расположенных:</p>
              <ol>
                
              </ol>`,

        '2': `<h2>Цель работы:</h2>
              <p>Алгоритмы с досрочным выходом из цикла. Алгоритмы обработки целых чисел. Способы ввода/вывода информации с использованием COM-порта микроконтроллеров Arduino.</p>
              <h2>Задание:</h2>
              <p>Задание данной лабораторной работы подразумевают разработку двух программ на языке C со стандартной работой через командную строку.</p>
              <h2>Часть 1:</h2>
              <p>Для решения задачи необходимо использовать алгоритмы с досрочным выходом из цикла.</p>
              <h2>Часть 2:</h2>
              <p>Дан массив целых положительных чисел.</p>
              <ol>

              </ol>`,

        '3': `<h2>Цель работы:</h2>
              <p>Использование процедур и функций.</p>
              <h2>Часть 1:</h2>
              <p>Оформить в виде функции общую часть в предложенных формулах:</p>
              <h2>Часть 2:</h2>
              <p>Вычислить значение по формуле:</p>
              <h2>Часть 3:</h2>
              <p>Дан целочисленный массив A[0:n-1]. Написать программу, включающую две функции с параметрами. В первой функции необходимо подсчитать количество повторений каждого элемента массива A.</p>
              <p>Вторая процедура решает следующую задачу:</p>
              <ol>

              </ol>`,

        '4': `<h2>Цель работы:</h2>
              <p>Обработка символьных строк.</p>
              <h2>Задание:</h2>
              <p>Дано k символьных строк. Каждая строка содержит латинские и русские буквы, цифры, а также все возможные разделители. Требуется:</p>
              <h2>Часть 1:</h2>
              <p>Выделить из каждой строки и напечатать подстроки:</p>
              <h2>Часть 2:</h2>
              <p>Среди выделенных подстрок найти подстроку (если таких подстрок несколько, выбирается первая из них):</p> 
              <h2>Часть 3:</h2>
              <p>Преобразовать исходную строку, которой принадлежит найденная подстрока, следующим образом:</p>
              <ol>

              </ol>`,

        '5': `<h2>Цель работы:</h2>
              <p>Программирование с использованием структурированных типов данных (файлы, структуры).</p>
              <h2>Часть 1:</h2>
              <p>Создать файл для хранения действительных чисел, вводимых с клавиатуры. Ввести данные.</p>
              <p>Прочитать этот файл, вывести данные на экран и вычислить:</p>
              <h2>Часть 2:</h2>
              <p>Создать текстовый файл средствами редактора (т.е. в «Блокноте»).</p>
              <p>Прочитать этот файл построчно и произвести в каждой строке следующие действия:</p>
              <ol>

              </ol>`,

        '6': `<h2>Цель работы 3:</h2>
              <p>Структуры. Файлы</p>
              <h2>Задание:</h2>
              <p>Ввод и вывод данных, а также формирование массива результатов оформить как отдельные функции. Проверку существования результата произвести в главной программе. К элементам массива структур обращаться при помощи индекса в первой программе и при помощи указателя во второй программе. При сравнении строк использовать стандартные функции для обработки символьных строк. Для ввода данных и вывода результатов использовать функции scanf, gets и printf, puts в первой программе и функции fscanf, fgets. fputs и fprintf во второй программе.</p>
              <p>Запрос выводится с новой строки для каждого поля.</p> 
              <h2>Часть 1:</h2>
              <p>После ввода массива структур программа ищет в нем следующую информацию (все данные в полученном массиве должны быть различны):</p>
              <h2>Часть 2:</h2>
              <p>Модифицировать программу, написанную в первом пункте, таким образом, чтобы она вводила структуры из текстового файла и записывала результаты в файл..</p>
              <ol>
                <li>Создать интерактивную форму</li>
                <li>Реализовать валидацию данных</li>
              </ol>`,

        '7': `<h2>Цель работы 1:</h2>
              <p>Связанные списки. Динамические переменные.</p>
              <h2>Задание:</h2>
              <p>Ввод данных, обработка списка отдельно для частей 2 и 3, а также вывод списка на экран оформляются как отдельные функции. Анализ существования результата производится в главной функции.</p>
              <p>При выделении и освобождении памяти использовать malloc и free.</p>
              <p>При вводе данных и выводе результата использовать fscanf, fgets и fprintf, fputs.</p>
              <h2>Часть 1:</h2>
              <p>Создать связанный список для хранения целых чисел. Число записей неизвестно, данные читаются из файла, имя которого задает пользователь. Имя выходного файла также задается пользователем. Входной файл содержит данные для исходного списка, выходной – для конечного списка. Для четных номеров вариантов организовать очередь, а для нечетных – стек.</p> 
              <h2>Часть 2:</h2>
              <p>Для исходного списка решить следующую задачу:</p> 
              <h2>Часть 3:</h2>
              <p>Для полученного списка решить следующую задачу:</p> 
              <ol>

              </ol>`,

        '8': `<h2>Цель работы 2:</h2>
              <p>Одномерные массивы. Двумерные массивы (с использованием указателей)</p>
              <h2>Часть 1:</h2>
              </p>Даны два одномерных массива целых чисел A и В. Вычислить одномерный массив С, который содержит:</p> 
              <h2>Часть 2:</h2>
              </p>В заданной целочисленной прямоугольной матрице поменять местами следующие два элемента.</p> 
              <ol>

              </ol>`,
              
        '9': `<h2>Цель работы 3:</h2>
              <p>Символьные строки. Подпрограммы</p>
              <h2>Задание:</h2>
              <p>Разработать программу для решения задачи, состоящей из трех частей. Все задания выполняются последовательно в одной программе. Каждое задание оформить в виде отдельной функции. Ввод и вывод данных (потоковый, данные вводятся с клавиатуры), а также проверку существования результата необходимо осуществлять в главной программе. При отсутствии результата предусмотреть вывод соответствующего сообщения.</p>
              <p>Даны символьные строки (количество строк задает пользователь). Требуется:</p>
              <h2>Часть 1:</h2>
              <p>Выделить из каждой строки и напечатать подстроки:</p>
              <h2>Часть 2:</h2>
              <p>Среди выделенных подстрок найти подстроку:</p>
              <h2>Часть 3:</h2>
              <p>Преобразовать исходную строку, которой принадлежит найденная подстрока, следующим образом:</p>
              <ol>
                
              </ol>`,

        '10': `<h2>Цель работы 3:</h2>
              <p>Списки. Динамические переменные</p>
              <h2>Задание:</h2>
              <p>Ввод данных, обработка списка и вывод результатов оформляются как отдельные функции. Анализ существования результата производится в главной функции. Результат (содержимое полученного списка) выводится в новый текстовый файл.</p>
              <h2>Часть 1:</h2>
              <p>Написать программу, которая вводит из файла структуры вида:</p>
              <p>Имя файла задает пользователь. Массив баллов B[0:4] содержит данные о результатах сдачи экзаменов, по 10-балльной шкале. Каждое поле структуры занимает в файле одну строку, а массив оценок размещается на отдельной строке. Средний балл не записан в файле, а вычисляется в процессе чтения данных. Структуры размещаются в:</p>
              <p>реализованном(ой) с помощью:</p>
              <h2>Часть 2:</h2>
              <p>Для полученного списка программа выполняет следующие действия:</p>
              <ol>

              </ol>`,

        '11': `<h2>Цель работы 11:</h2>
               <p>Финальный проект.</p>
               <h2>Задание:</h2>
               <ol>
                 <li>Реализовать SPA приложение</li>
                 <li>Использовать все изученные технологии</li>
               </ol>`
    };
    
    document.getElementById('lab-description').innerHTML = descriptions[labNumber] || '<p>Описание для этой работы пока не добавлено</p>';

    document.getElementById('variant').addEventListener('input', function() {
        loadTasks(labNumber, this.value);
    });
}

function loadTasks(labNumber, variant) {
    const tasksContainer = document.getElementById('tasks-container');
    tasksContainer.innerHTML = '';
    
    if (!variant) {
        tasksContainer.innerHTML = '<p>Введите вариант, чтобы увидеть задания</p>';
        return;
    }
    
    const variantNumber = parseInt(variant, 10);
    if (isNaN(variantNumber)) {
        tasksContainer.innerHTML = '<p>Некорректный номер варианта</p>';
        return;
    }
    
    const labData = tasksByLab[labNumber];
    if (!labData) {
        tasksContainer.innerHTML = '<p>Задания для этой работы не найдены</p>';
        return;
    }
    
    let tasksHTML = '<h3>Ваши задания:</h3><ol>';
    
    for (let part = 1; part <= labData.parts; part++) {
        const partKey = `part${part}`;
        const taskText = labData.templates[partKey](variantNumber);
        tasksHTML += `<li>${taskText}</li>`;
    }
    
    tasksHTML += '</ol>';
    tasksContainer.innerHTML = tasksHTML;
}

function submitSolution() {
    const labNumber = document.getElementById('lab-number').textContent;
    const variant = document.getElementById('variant').value;
    const code = document.getElementById('code').value;
    
    if (!variant || !code) {
        alert('Пожалуйста, заполните все поля');
        return;
    }
    
    const isSuccess = Math.random() > 0.3; // Заглушка для демонстрации

    addToHistory(labNumber, isSuccess, code);
    document.getElementById('code').value = '';
}

function addToHistory(labNumber, isSuccess, code) {
    const historyItem = document.createElement('div');
    historyItem.className = `history-item ${isSuccess ? 'success' : 'error'}`;
    
    const now = new Date();
    const timestamp = now.toLocaleString();
    
    historyItem.innerHTML = `
        <div class="status">${isSuccess ? '✓ Решение верное' : '✗ Ошибка в решении'}</div>
        <div class="timestamp">${timestamp}</div>
        <pre class="code-snippet">${code.substring(0, 100)}${code.length > 100 ? '...' : ''}</pre>
    `;
    
    document.getElementById('history-list').prepend(historyItem);
    saveToLocalStorage(labNumber, isSuccess, code, timestamp);
}

function saveToLocalStorage(labNumber, isSuccess, code, timestamp) {
    const history = JSON.parse(localStorage.getItem(`lab_${labNumber}_history`) || '[]');
    
    const newHistory = [{
        success: isSuccess,
        code: code,
        timestamp: timestamp
    }, ...history.slice(0, 9)]; 

    localStorage.setItem(`lab_${labNumber}_history`, JSON.stringify(newHistory));
}

function loadSolutionHistory(labNumber) {
    const history = JSON.parse(localStorage.getItem(`lab_${labNumber}_history`) || []);
    const historyList = document.getElementById('history-list');
    
    historyList.innerHTML = '';
    
    if (history.length === 0) {
        historyList.innerHTML = '<p>Здесь будет отображаться история ваших решений</p>';
        return;
    }
    
    history.forEach(item => {
        const historyItem = document.createElement('div');
        historyItem.className = `history-item ${item.success ? 'success' : 'error'}`;
        
        historyItem.innerHTML = `
            <div class="status">${item.success ? '✓ Решение верное' : '✗ Ошибка в решении'}</div>
            <div class="timestamp">${item.timestamp}</div>
            <pre class="code-snippet">${item.code.substring(0, 100)}${item.code.length > 100 ? '...' : ''}</pre>
        `;
        
        historyList.appendChild(historyItem);
    });
}